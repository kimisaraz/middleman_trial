---
pageable: true
per_page: 10
---
- if paginate && num_pages > 1
  %p
    Page #{page_number} of #{num_pages}
- page_articles.each_with_index do |article, i|
  %h2
    = link_to article.title, article
    %span= article.date.strftime('%b %e')
  = article.summary
  = link_to 'Read more…', article
- if paginate
  %nav
    %ul.pager
      - if prev_page
        %li.previous
          = link_to '&larr; Previous page', prev_page, rel: "prev"
      - if next_page
        %li.next
          = link_to 'Next page &rarr;', next_page, rel: "next"
