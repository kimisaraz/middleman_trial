---
layout: layout
pageable: true
per_page: 12
---
%article.article
  %header.article-header
    %h1.article-header__title
      Articles tagged &lsquo;#{tagname}&rsquo;
  - if paginate && num_pages > 1
    %p.page-number
      Page #{page_number} of #{num_pages}
  .article-body.archive
    %ul.archive__list
      - page_articles.each_with_index do |article, i|
        %li.archive__item
          = link_to article.title, article
          %time.archive__date-published{datetime: article.date.iso8601}
            = article.date.strftime('%b %d, %Y')
  %footer.article-footer
    - if paginate
      %ul.pager
        - if prev_page
          %li.previous
            = link_to '&larr; Previous page', prev_page, rel: "prev"
        - if next_page
          %li.next
            = link_to 'Next page &rarr;', next_page, rel: "next"
